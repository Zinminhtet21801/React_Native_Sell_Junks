import React from 'react'
import { View, Text } from 'react-native'

const CategoryPicker = ({ items }) => {
    return (
        <View>
            <Text></Text>
        </View>
    )
}

export default CategoryPicker
